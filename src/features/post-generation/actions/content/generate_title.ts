 "use server";

import { Keyword, Persona } from "../../types";


export async function generateTitle(
  keyword: Keyword,
  persona: Persona
)
 {
  // 응답 데이터
  return {
    subkeyword: subkeyword,
    title: title
  };
}
